/**
 * An enumerator to indicate the result of test helper functions.
 *
 * @since 1.0.0
 *
 * @type {{VALID: string, INVALID: string, DEFAULT: string}}
 */
const TestResult = {
    VALID :     'valid',
    INVALID :   'invalid',
    DEFAULT :   'default'
};

module.exports = TestResult;
