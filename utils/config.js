const Validation = require('./validation.js');

module.exports = {
    svg_drawing: {
        color: 'black',
        maxX: Validation.DEFAULT_SPAN_VALUE,
        maxY: Validation.DEFAULT_SPAN_VALUE,
    }
};